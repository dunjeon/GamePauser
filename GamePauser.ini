; GamePauser configuration
; Hotkey to pause/resume the current foreground process

; Examples of valid keys: P, F1, F12, F24, Z (any key really)
; Valid modifiers: Ctrl, Alt, Shift, Win (combine with +)
; Single modifiers is also valid (Alt Z) for example

PauseKey = P
Modifiers = Ctrl+Alt

